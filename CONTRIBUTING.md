# Contributing to zedom

Thanks for your interest in contributing to zedom!

## Development Setup

```bash
pnpm install
pnpm test:run
```

## Pull Request Process

1. Fork the repository and create a feature branch
2. Write or update tests for your changes
3. Ensure `pnpm lint:check` and `pnpm test:run` pass
4. Update documentation if needed
5. Submit a pull request

## Code Style

- TypeScript with strict mode
- 2-space indentation
- Single quotes
- No semicolons
- CamelCase for variables and functions

## Commit Messages

Follow conventional commit format:
- `feat:` new feature
- `fix:` bug fix
- `docs:` documentation
- `refactor:` code refactoring
- `test:` test changes
- `chore:` maintenance tasks

## Questions

Open a [GitHub Discussion](https://github.com/xypur/zedom/discussions) or issue.
